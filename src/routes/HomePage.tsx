// Intentionally empty — the search box and filters above (owned by AppShell)
// are the entire home experience. This is the empty landing state shown
// when nothing is being searched or filtered yet.
export function HomePage() {
  return null
}
