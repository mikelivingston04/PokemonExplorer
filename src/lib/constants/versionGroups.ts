// This app is themed around playing FireRed — TM numbers, item locations, and
// encounter data are scoped to this version group by default, even though the
// underlying Pokémon roster shown can span any generation the user filters to.
export const DEFAULT_VERSION_GROUP = 'firered-leafgreen'
